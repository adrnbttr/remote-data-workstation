---
dev_tools_home: "/home/{{ workstation_user }}"

# Base system packages, including the build dependencies pyenv needs to compile
# CPython, and libpq for psycopg.
dev_tools_base_packages:
  - build-essential
  - ca-certificates
  - curl
  - wget
  - git
  - unzip
  - gnupg
  - lsb-release
  - pkg-config
  - jq
  - libssl-dev
  - zlib1g-dev
  - libbz2-dev
  - libreadline-dev
  - libsqlite3-dev
  - libffi-dev
  - liblzma-dev
  - libpq-dev
