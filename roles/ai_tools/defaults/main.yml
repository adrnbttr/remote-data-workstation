---
ai_tools_home: "/home/{{ workstation_user }}"

# Claude Code CLI. Authentication is intentionally left manual (a one-time
# interactive login), so no credentials are ever provisioned onto the VM.
ai_tools_install_claude_code: true
ai_tools_claude_bin: "{{ ai_tools_home }}/.local/bin/claude"
