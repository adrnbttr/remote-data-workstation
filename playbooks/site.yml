---
# Personal profile — provisions the base ("socle") workstation, with no project
# overlay. Roles are wired in incrementally; see the roadmap in the README.
- name: Provision base workstation
  hosts: workstation
  become: true

  roles:
    - user
